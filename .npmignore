# Build artifacts - exclude entire directories recursively
# Note: npm .npmignore patterns work relative to package root
build/
install/
.gradle/
libs/
include/
dist/

# Platform-specific build artifacts (explicit paths)
android/build/
android/install/
android/.gradle/
android/local.properties
ios/build/
ios/install/
# Note: ios/libs/ and ios/include/ are KEPT - they contain pre-built libraries needed by podspec

# Binary libraries - exclude from build/install directories only
# But KEEP ios/libs/*.a and ios/include/ (needed by podspec)
android/build/**/*.a
android/build/**/*.so
android/install/**/*.a
android/install/**/*.so
ios/build/**/*.a
ios/build/**/*.dylib
ios/install/**/*.a
ios/install/**/*.dylib
*.framework
*.xcframework
**/*.framework
**/*.xcframework

# Source dependencies (not needed in package)
ref-code/
**/ref-code/
ngtcp2/
nghttp3/
openssl/
openssl-*/
ngtcp2-*/
nghttp3-*/

# iOS build artifacts
ios/install/
ios/build/
# Note: ios/libs/ and ios/include/ are KEPT for vendored_libraries in podspec
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Android build artifacts
android/install/
android/build/
android/.gradle/
android/local.properties
*.apk
*.ap_
*.aab
*.dex
*.class

# CMake artifacts (anywhere)
**/CMakeCache.txt
**/CMakeFiles/
**/cmake_install.cmake
**/Makefile
**/*.cmake
!**/CMakeLists.txt

# Development files
.git/
.gitignore
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
*.temp
*.bak
*.backup
*.orig

# Test artifacts
coverage/
.nyc_output/
*.lcov

# Environment files
.env
.env.local
.env.*.local

# Documentation (keep README.md and LICENSE if present)
NGTCP2_INTEGRATION_PLAN.md
NGTCP2_IMPLEMENTATION_STATUS.md
IMPLEMENTATION_SUMMARY.md
MQTT5_IMPLEMENTATION_COMPLETE.md
android/NGTCP2_BUILD_INSTRUCTIONS.md
ios/NGTCP2_BUILD_INSTRUCTIONS.md

# Keep these (explicitly included in package.json files field)
!ios/
!android/
!dist/
